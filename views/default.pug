extends adminLayout

block css 
    link(id="favicon" rel="icon" href="/img/MailPickupAgent.png" type="image/png")
    
block content
              
    form(action='/SMTPLog', method="POST")
        input(type='hidden', name='_csrf', value=csrfToken)
        div Select a menu item on the left.